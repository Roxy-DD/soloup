# Soloup 前端构建期环境变量（复制为 .env.local 使用）
#
# NEXT_PUBLIC_SOLOUP_API —— 后端 RPC 地址的**强制覆盖**。
#
# 【默认不要设置】留空即可。api.ts 的 hostOf() 会在运行时按「当前网页所在主机」
# 反推后端地址，这才能同时满足三种场景：
#   - 浏览器直连后端托管页（http://<随便什么IP>:8787/）→ 同源，手机也能用
#   - next dev（http://<主机>:3000）                    → 同主机的 8787
#   - Tauri 壳（tauri.localhost）                       → 回落 localhost:8787
#
# 一旦在这里写死，值会在构建期被内联进 JS bundle，运行时反推逻辑全部失效：
# 手机上打开页面会去连手机自己的 localhost:8787（永远连不上）。
# 只有当后端跑在**非本机、也非页面所在主机**时，才需要手动指定。
#
# NEXT_PUBLIC_SOLOUP_API=http://192.168.1.11:8787/api/rpc
